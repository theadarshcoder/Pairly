# Inline Style Hex-Code Audit — Issue 28

Generated by scanning `apps/web/src/` for hardcoded hex values outside `tokens.css`.

## Summary

The majority of hex codes in component files are **CSS fallback values** inside
`var(--token, #fallback)` syntax. These are intentional — they ensure components
render acceptably if CSS custom properties haven't loaded yet.

## Pre-existing Files (need review)

| File | Line | Hex | Status |
|------|------|-----|--------|
| `Button.tsx` | 67, 86 | `#ffffff` | TODO(tokens): should be `var(--fg)` or a dedicated `--fg-on-accent` |
| `SlideErrorBoundary.tsx` | 132 | `#fff` | TODO(tokens): button text on accent — needs `--fg-on-accent` token |
| `Badge.tsx` | — | needs audit | TODO(tokens): check for any raw hex |
| `Modal.tsx` | — | needs audit | TODO(tokens): check for any raw hex |
| `ConnectionStatusBar.tsx` | — | uses var() | ✅ Already token-based |

## New Files (all clean)

All new files created during this scaffolding pass use `var(--token, #fallback)` syntax.
The hex values are CSS fallbacks only, not primary color specifications.

Files verified clean:
- `SlideTitle.tsx` — var() with fallbacks ✅
- `ConnectionDot.tsx` — var() with fallbacks ✅
- `BottomSheet.tsx` — var() with fallbacks ✅
- `Toast.tsx` — var() with fallbacks ✅
- `PinInput.tsx` — var() with fallbacks ✅
- `QRJoinCode.tsx` — var() with fallbacks ✅
- `RewardCheck.tsx` — var() with fallbacks ✅
- `PresentModeExit.tsx` — var() with fallbacks ✅
- `SlideErrorBoundary.tsx` — var() with fallbacks ✅ (except line 132: `#fff`)
- `PresenterDock.tsx` — var() with fallbacks ✅
- `PresenterTopBar.tsx` — var() with fallbacks ✅
- Dashboard page shells — var() with fallbacks ✅

## Action Items

1. Add `--fg-on-accent: #fff` to `tokens.css` for text on colored buttons
2. Migrate `Button.tsx` lines 67, 86 from `#ffffff` → `var(--fg-on-accent)`
3. Migrate `SlideErrorBoundary.tsx` line 132 from `#fff` → `var(--fg-on-accent)`
4. Audit `Badge.tsx` and `Modal.tsx` for any remaining raw hex codes
